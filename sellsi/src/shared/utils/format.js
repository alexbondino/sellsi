// DEPRECATED: este archivo era un duplicado. Usar los formatters centralizados en:
// src/shared/utils/formatters
// Se deja un re-export mínimo para compatibilidad temporal.
export { formatNumber, formatCurrency } from './formatters'

// Nota: borrar este archivo es seguro una vez que no haya importaciones que lo referencien.
